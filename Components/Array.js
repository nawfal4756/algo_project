import {
  TableBody,
  TableCell,
  TableContainer,
  TableRow,
  Typography,
} from "@mui/material";
import Head from "next/head";
import classes from "../styles/Array.module.css";

export default function Array() {
  return (
    <Head>
      <Typography variant="h1">Array</Typography>
    </Head>
  );
}
